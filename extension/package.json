{
  "name": "agent-orbit-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
